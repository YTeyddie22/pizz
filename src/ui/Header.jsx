import { Link } from "react-router-dom";

const Header = () => {
    return (
        <header>
            <Link to="/">Pizz Co.</Link>
            <p>Teddy</p>
        </header>
    );
};

export default Header;
